rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public catalog read; tighten `write` before launch (e.g. admin-only).
    match /products/{productId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
