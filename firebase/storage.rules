rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Public read for product images under products/
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
